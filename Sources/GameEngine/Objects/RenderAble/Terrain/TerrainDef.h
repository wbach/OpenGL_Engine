#pragma once

static const float TERRAIN_SIZE = 64.f;
static const float TERRAIN_SCALE = 10.f;
static const float TERRAIN_HEIGHT_FACTOR = 6.f * TERRAIN_SCALE;
static const float TERRAIN_TOTAL_SIZE = TERRAIN_SIZE * TERRAIN_SCALE;
static const float TERRAIN_HALF_TOTAL_SIZE = TERRAIN_TOTAL_SIZE / 2.f;